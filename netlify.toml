# Ücretsiz Netlify deploy (Vercel Pro istemez)
# Netlify → Add new site → Import from Git → hfrz-27/mepotia → main

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
